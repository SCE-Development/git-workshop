version: '3'
services:
  # recipe-nginx:
  #   image: nginx:alpine
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./:/usr/share/nginx/html:ro
  app:
    container_name: app
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: 'always'
    ports:
        - 3000:3000

# networks:
#   default:
#     external:
#       name: sce